# 项目设计规范

本文档用于记录和维护项目的设计规范，会在开发过程中不断更新。

## 目录
- [布局规范](#布局规范)
- [颜色规范](#颜色规范)
- [组件规范](#组件规范)
- [字体规范](#字体规范)
- [响应式规范](#响应式规范)

## 布局规范

### 整体布局
- 页面采用左侧侧边栏 + 右侧内容区的布局
- 侧边栏宽度固定为 `120px`
- 内容区与侧边栏间距为 `16px`
- 内容区内填充为 `16px 24px`（左右内边距为24px）

### 顶部导航
- 顶部导航组件位于内容区顶部
- 导航总高度为：`24px` + 上下边距
- 左侧为面包屑导航，右侧为用户信息和通知区域
- 右侧区域使用白色背景 `#FFFFFF`，圆角 `24px 2px 2px 24px`，轻微阴影 `0px 1px 2px rgba(0, 0, 0, 0.05)`
- 组件间使用垂直分隔线隔开，分隔线高度 `20px`，颜色 `#E4E4E4`
- 各子区域间距为 `4px`（gap）

### 卡片布局
- 卡片外边距：`20px`
- 卡片内边距：`20px`
- 卡片边框：`1px solid #EEEEEE`
- 卡片圆角：`8px`

### 统计数据区域
- 顶部统计卡片内边距：`30px 40px`
- 数据单元内部间距：`56px`

## 颜色规范

### 主要颜色
- 主题色：`#155BD4`（按钮和重要操作）
- 侧边栏背景色：`#202033`
- 页面背景色：`#F7F7F7`
- 卡片背景色：`#FFFFFF`
- 分隔线颜色：`#E4E4E4`

### 文本颜色
- 主要文本：`#333333`
- 次要文本：`#666666`
- 辅助/说明文本：`#999999`
- 导航文本：`#323233`（顶部组件中的文本）

### 边框颜色
- 边框：`#EEEEEE`

## 组件规范

### 组件库
- 使用 Ant Design 组件库

### 导航组件
- 面包屑导航使用 `14px` 字体，当前页面使用黑色 `#333333`，上级页面使用灰色 `#999999`
- 右侧区域整体使用白色背景 `#FFFFFF`，圆角 `24px 2px 2px 24px`，阴影 `0px 1px 2px rgba(0, 0, 0, 0.05)`
- 智能助手组件使用圆角 `24px` 的浅灰背景 `#F8F8F8`
- AI图标使用渐变背景 `linear-gradient(180deg, #292B31 53.48%, #585A62 100%)`，带有特殊阴影 `0px 0px 5.8px rgba(129, 147, 183, 1), inset 0px -4px 4px rgba(0, 0, 0, 0.05)`
- 徽标使用红色背景 `#D42F15`，无阴影
- 用户头像使用灰色背景 `#D8D8D8`，边框 `1px solid #999999`，尺寸 `24px`

### 按钮
- 主要按钮使用主题色 `#155BD4`
- 次要按钮使用白色背景，主题色边框
- 文字按钮使用主题色文字，无背景

### 卡片
- 卡片统一使用白色背景
- 悬停效果采用轻微阴影变化

### 数据展示
- 数据标题文字采用 `14px`，颜色 `#999999`
- 数据值采用 `24px`，颜色 `#333333`，字重 `500`
- 数据单位采用 `14px`，颜色 `#999999`

## 字体规范

### 标题
- 一级标题：`24px`，字重 `500`，颜色 `#333333`
- 二级标题：`16px`，字重 `500`，颜色 `#333333`

### 正文
- 主要正文：`14px`，颜色 `#333333`
- 辅助文本：`14px`，颜色 `#666666`
- 说明文本：`12px`，颜色 `#999999`

### 数据文本
- 大型数据：`24px`，字重 `500`，颜色 `#333333`
- 中型数据：`18px`，字重 `500`，颜色 `#333333`

## 响应式规范

- 桌面端基准宽度：`1280px`
- 列间隔：`20px`（普通卡片）和 `56px`（统计数据）

*注意：本文档将随着项目发展持续更新。每次讨论中产生的新规范都会被记录在此。* 